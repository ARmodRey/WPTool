#pragma once

#include <string>
#include <cstdio>

int qual_in_str(std::string, char);
bool is_digits(std::string);
char sim_in_str(std::string, std::string);
int get_start_index(std::string, std::string);